extends ../../layouts/default.pug

block main
  .container.my-5
    .row.justify-content-center
      .col-8
        h1(class="text-center") Đổi mật khẩu
        form(
          action="/user/reset-password"
          method="POST"
        )
          div(class="form-group")
            label(for="password") Mật khẩu mới
            input(
              type="password"
              class="form-control new-pass"
              id="password"
              name="password"
              required
            )
          div(class="form-group")
            label(for="confirmPassword") Xác nhận mật khẩu
            input(
              type="password"
              class="form-control"
              id="confirmPassword"
              name="confirmPassword"
              required
            )
            div(class="alert alert-danger mt-1 p-1 d-none alert-confirm" role="alert")  Mật khẩu xác nhận chưa trùng khớp!
          div(class="form-group mt-2")
            button(
              type="submit"
              class="btn btn-primary btn-block"
            ) Đổi mật khẩu