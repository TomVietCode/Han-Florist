mixin sub-menu(records, className="menu-nav")
  ul(class=className)
    each item in records 
      li 
        a(href=`/products/${item.slug}`) #{item.title}
        if(item.children && item.children.length > 0)
          +sub-menu(item.children, className="sub-menu")
